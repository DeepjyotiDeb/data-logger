# Data logger
Uses a IR Sensor connected to a ESP8266. A simple counter stores the number of times ir sensor is being triggered. 
This data is stored in a text file stored using the SPIFFS file system and can be retrieved by the user by entering "print" in the Serial Monitor
